# Trap

Execute command when shell receives signal

```bash
trap "<command>" <signal>[ ...]
```

Execute on shell exit

```bash
trap "<command>" { EXIT | 0 }
```

Show associated commands

```bash
trap -p
```

Show signal list

```bash
trap -l
```
